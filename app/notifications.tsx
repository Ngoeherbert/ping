import { ScreenPlaceholder } from '@/components/ui/ScreenPlaceholder';

export default function Screen() {
  return <ScreenPlaceholder title="Notifications" subtitle="Likes, comments, follows, and mentions" />;
}
